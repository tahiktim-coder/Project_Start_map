/* Wrapper: renders the shipped src/systems/dither/StructureArt.js through the harness (a stub window stands in for the browser). */
'use strict';
global.window = global.window || {};
require('../../src/systems/dither/StructureArt.js');
module.exports = Object.assign({ name: 'shipped' }, window.StructureArt);
